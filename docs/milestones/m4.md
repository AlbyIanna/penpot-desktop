# M4 — Packaging

## Goal (from PLAN.md)

jlink-minimized JRE, pinned Penpot release fetch, AppImage + dmg, Nix flake (dev shell +
package). Orchestration addition: the packaged app's FIRST boot must be fully offline —
in dev, `postgresql_embedded` downloads postgres binaries from GitHub on first run; the
bundle must pre-seed them.

## Exit criteria (from PLAN.md)

A fresh machine (or clean NixOS VM) runs the app from a single artifact.

## Component checklist

- [x] **Runtime bundle builder** (`scripts/build-runtime-bundle.sh`, Bundle phase):
      produces `dist/penpot-runtime/` per the layout contract — `backend/` (penpot.jar
      2.16.2 + builtin-templates + scripts), `frontend/` (static SPA), `jre/` (jlink,
      33 modules, mirrors the upstream image's module set, 71.3 MB), `bin/`
      (valkey-server 9.1.0, relocated ImageMagick `identify` + coder modules,
      penpot-watchdog), `postgres/15.18.0/` (ready postgresql_embedded install),
      `licenses/`, `MANIFEST.json`, `VERSION`. All binaries relocated to
      `@loader_path` dylibs, re-signed, proven homebrew-free (otool audit +
      sandbox-exec denying /opt/homebrew reads). `node` deliberately NOT bundled —
      on 2.16.2 the only node call-site (`app.svgo`) has zero callers; verified
      statically and live (SVG upload with node hidden → 200). Idempotent
      (fingerprint skip + proofs re-run every time). Total 562.7 MiB.
- [x] **Runtime layout resolver** (`apps/desktop/src/layout.rs`, Resolution phase):
      fixed precedence env override → bundled `penpot-runtime/` → dev default, per
      component (runtime dir, java, valkey, postgres install, watchdog, backend child
      PATH). Bundle discovery: `PENPOT_LOCAL_RUNTIME_BUNDLE` (hard-fails if invalid) →
      Tauri resources dir → executable-adjacent. Pre-seeded postgres poisons
      `releases_url` so a download attempt fails instantly = offline by construction.
      Dev mode with no bundle + no new env vars is byte-identical to pre-M4.
- [x] **Tauri bundler config** (this phase): `apps/desktop/tauri.conf.json` — bundling
      active, targets `["app", "dmg"]`, identifier `dev.albyianna.penpot-local`,
      version derived from Cargo.toml (workspace `0.1.0`), full icon set generated
      from the repo icon (`cargo tauri icon`, source upscaled to 1024² via sips),
      ad-hoc signing (`signingIdentity: "-"`). The `penpot-runtime` resources live in
      an **overlay config** `apps/desktop/tauri.bundle.conf.json` applied only by the
      build script — see "dev-mode protection" below.
- [x] **Build script** (`scripts/build-dmg.sh`): runs `build-runtime-bundle.sh`
      (idempotent) then `CI=true cargo tauri build --config tauri.bundle.conf.json`;
      reports sizes + signature.
- [x] **Fresh-machine-approximation test** (`scripts/m4-artifact-test.sh`): 19/19 PASS
      (evidence below).
- [x] **Dev suites still green**: `cargo test --workspace` 205 passed / 0 failed /
      8 ignored; `scripts/m1-smoke.sh` all pass (2026-07-13, after all M4 changes).
      m2/m3 scripts belong to the Verify phase.
- [ ] **AppImage (Linux)**: NOT built — no Linux machine in this environment;
      `build-runtime-bundle.sh` guards macOS-arm64-only and needs a parallel Linux
      implementation.
- [ ] **Nix flake**: `flake.nix` + `nix/` authored by the CI phase; Nix is not
      installed on this machine, validation happens in CI only.

## The artifacts

| artifact | path | size |
|---|---|---|
| dmg | `target/release/bundle/dmg/Penpot Local_0.1.0_aarch64.dmg` | 386.2 MiB |
| .app | `target/release/bundle/macos/Penpot Local.app` | 608 MiB (incl. dereferenced symlinks) |
| runtime bundle | `dist/penpot-runtime/` | 562.7 MiB |

Timings on this machine (M-series, warm caches): `build-dmg.sh` end-to-end ≈ 60 s when
the runtime bundle fingerprint matches (bundle proofs ~13 s + release compile ~26 s +
resource copy + ad-hoc sign + dmg); cold release compile adds ~1 m; a from-scratch
runtime bundle build adds ~4 min. Artifact test end-to-end ≈ 40 s (offline first boot
alone: **14 s** to a fully provisioned, RPC-answering stack).

## Dev-mode protection (why the resources overlay exists)

`tauri-build` copies `bundle.resources` next to the compiled binary so `tauri dev` can
resolve them. If the resources lived in the base `tauri.conf.json`, every **debug**
build would drop a `penpot-runtime/` into `target/debug/`, and the executable-adjacent
bundle discovery in `layout.rs` would silently flip every dev run (`cargo run`,
m1/m2/m3 scripts) into packaged mode. The overlay config is applied only by
`scripts/build-dmg.sh`; verified after a debug build that `target/debug/penpot-runtime`
does not exist. (The release build does get `target/release/penpot-runtime` — that copy
is cleared and recreated by the build script; see fix #4 below.)

## Evidence — `scripts/m4-artifact-test.sh`, 19/19 PASS (2026-07-13)

Fresh-machine approximation: mount the dmg (`hdiutil`), `ditto` the .app to a scratch
install dir, run `Contents/MacOS/penpot-desktop` directly under
`env -i HOME=<fresh tmp> PATH=/usr/bin:/bin:/usr/sbin:/sbin` with
`http_proxy/https_proxy/HTTP_PROXY/HTTPS_PROXY/ALL_PROXY=http://127.0.0.1:1` (dead
port — anything needing the network fails loudly), fresh
`PENPOT_LOCAL_DATA_DIR`, ports 8906/6381/5455/6398.

- (a) full boot: SPA through the proxy; `/__bootstrap` consumed by the webview
  (single-use auto-login — the 403 marker body is the positive evidence); authenticated
  `get-profile` with the persisted token; PNG upload → byte-identical fetch through
  `/assets/**` — this execs the **bundled relocated `identify`** via the backend child
  PATH.
- (b) OFFLINE first boot: all 6 runtime-layout log lines `source=bundle` (0
  `source=dev`); no `<data>/postgres/install` created; zero `download` lines in the
  boot log; READY in 14 s under fully poisoned proxies.
- (c) provenance: `lsof` over every stack pid (app, java, valkey, postgres, watchdog)
  → **0** opens under `/opt/homebrew`, **0** under the repo checkout; watchdog armed
  from `<.app>/Contents/Resources/penpot-runtime/bin/penpot-watchdog`.
- (d) sync-status tray created (GUI run — a real window appears; this is not a
  headless CI-able test as-is).
- (e) SIGTERM → clean exit ≤ 20 s, no orphans; second boot (same data dir) → SIGKILL →
  watchdog reaps every child ≤ 30 s.

## Failures found and fixed on the way

1. **`build-runtime-bundle.sh` exited 1 on its skip path**: the EXIT trap's last
   command (`[[ -n "$STAGING" ]] && rm`) fails under `set -e` when staging is empty.
   Fixed with an explicit `return 0` in `cleanup()`.
2. **dmg named `x64` on an arm64 machine**: `cargo-tauri` was installed as an x86_64
   (Rosetta) binary and names the dmg after its own arch. Reinstalled tauri-cli 2.11.4
   native arm64 → `Penpot Local_0.1.0_aarch64.dmg`.
3. **`bundle_dmg.sh` failed headless**: the dmg step drives Finder via AppleScript for
   window layout. `CI=true` (in `build-dmg.sh`) passes `--skip-jenkins`, skipping the
   AppleScript.
4. **Second build: "Permission denied (os error 13)"**: tauri-build re-copies resources
   over `target/release/penpot-runtime`, and `std::fs::copy` cannot overwrite the
   bundle's 34 read-only files (jre legal docs, license texts). `build-dmg.sh` clears
   the stale copy first.
5. **Real product bug — env proxies hijacked loopback RPC** (found by the
   poisoned-proxy run): `reqwest::Client::new()` honors `http_proxy` & co., so on any
   machine with a proxy configured the app's own `login`/`register-profile` calls to
   `127.0.0.1` would go through the proxy and first boot would fail. Fixed:
   `PenpotClient` now builds its client with `.no_proxy()` (it only ever talks to the
   local backend). The Java backend, valkey, postgres and the supervisor's raw-TCP
   probes were already immune.
6. **App was only linker-signed**: Tauri does not sign without an identity; set
   `signingIdentity: "-"` → proper ad-hoc bundle signature
   (`codesign --verify --deep --strict` passes, identifier
   `dev.albyianna.penpot-local`).
7. **Test-harness lessons**: `/__bootstrap` is single-use per boot and the GUI webview
   wins the race — readiness polling must use `GET /`; the watchdog's spawned path is
   non-canonicalized (`Contents/MacOS/../Resources/...`), assert on suffix.

## Gatekeeper / notarization (documented consequence)

M4 ships ad-hoc signed, NOT notarized. On any other Mac, a downloaded dmg carries the
quarantine attribute and double-click-open is blocked ("cannot verify the developer").
First launch requires **right-click → Open** (twice on newer macOS: System Settings →
Privacy & Security → "Open Anyway"), or `xattr -d com.apple.quarantine`. A Developer ID
+ notarization is the M5+ answer. Local builds (this machine) have no quarantine bit,
so the artifact test cannot and does not exercise Gatekeeper itself.

## Honest platform coverage statement

**Verified here: macOS arm64 (Apple Silicon) only.** The dmg is a thin arm64 build made
and tested on the dev machine (macOS 15/Darwin 25). The "fresh machine" is an
*approximation* — sanitized env, fresh HOME, poisoned proxies, provenance lsof audit —
not a literal separate machine; SIP-protected system frameworks, the WindowServer
session, and the logged-in GUI user are shared with the dev box, and Gatekeeper's
quarantine flow was reasoned about, not executed. Intel macOS (x86_64), the Linux
AppImage, and the Nix package are **not** built or tested by this phase: the bundle
builder is macOS-arm64-only by explicit guard, and the Nix flake is validated
exclusively in CI (no local nix). The exit criterion "a fresh machine runs the app from
a single artifact" is met for Apple Silicon Macs modulo the right-click-Open
Gatekeeper step.

## Test-port ledger addition

8906/6381/5455/6398 (`m4-artifact-test.sh`, freed after each run).
